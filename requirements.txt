# requirements.txt
# ────────────────
# Python dependencies for the movie recommender system.
# Install with: pip install -r requirements.txt

streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
requests>=2.31.0

# Optional: for loading .env files locally
python-dotenv>=1.0.0
